{
  "name": "flc-budget-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "FLC Budget Tracker — full-stack (React + Express + SQLite)",
  "engines": { "node": "20.x" },
  "scripts": {
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && npm --prefix server install --omit=dev",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server start",
    "dev:client": "npm --prefix client run dev"
  }
}
